public interface IDataComponent
{
	/*
		This class should only contain data
		No functions
		We should only primitives and data structures like dictionaries, lists or indexes of data
		But sometimes we store an IExternalObjectWrapper, if this is our main bucket of state
	 */
}
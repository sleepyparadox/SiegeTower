@namespace SiegeTower.Client.UX

<div class="loading-queue @(IsLoading ? "loading-queue-visible" : "")" role="status" aria-live="polite">
	<i class="fa-solid fa-spinner fa-spin" aria-hidden="true"></i>
</div>

@code {
	[Parameter]
	public bool IsLoading { get; set; }
}
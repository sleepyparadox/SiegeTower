@namespace SiegeTower.Client.Views.Components

<span class="grid-center-vertically grid-wrap-x status-item">@Item.Text</span>

@code {
    [Parameter, EditorRequired]
    public StatusItem Item { get; set; } = default!;
}
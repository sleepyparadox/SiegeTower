@namespace SiegeTower.Client.Views.Components

<button class="toolbar-button button grid-padded" type="button" @onclick="InvokeAction">@Control.Text</button>

@code {
    [Parameter, EditorRequired]
    public ButtonControl Control { get; set; } = default!;

    [Parameter, EditorRequired]
    public Session Session { get; set; } = default!;

    void InvokeAction()
    {
        Control.Action?.Invoke(Session);
    }
}
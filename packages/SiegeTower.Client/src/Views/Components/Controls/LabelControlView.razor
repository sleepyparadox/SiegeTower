@namespace SiegeTower.Client.Views.Components

<span class="grid-center-vertically grid-wrap-x label">@Control.Value</span>

@code {
    [Parameter, EditorRequired]
    public LabelControl Control { get; set; } = default!;
}
@namespace SiegeTower.Client.Views.Components

<header class="grid-line grid-center-vertically subwindow-title-bar">
    @ChildContent
</header>

@code {
    [Parameter, EditorRequired]
    public SubwindowTitleBar TitleBar { get; set; } = default!;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
@namespace SiegeTower.Client.Views.Components

<div class="toolbar-row grid-line grid-row-layout grid-width-fit grid-center-vertically">
    @ChildContent
</div>

@code {
    [Parameter, EditorRequired]
    public ToolbarRow ToolbarRow { get; set; } = default!;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
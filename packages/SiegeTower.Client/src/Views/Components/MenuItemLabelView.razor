@namespace SiegeTower.Client.Views.Components

<span class="menu-item-label grid-wrap-x">@Label.Text</span>

@code {
    [Parameter, EditorRequired]
    public MenuItemLabel Label { get; set; } = default!;
}
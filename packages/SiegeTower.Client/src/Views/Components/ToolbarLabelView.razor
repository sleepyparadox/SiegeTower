@namespace SiegeTower.Client.Views.Components

<span class="grid-width-fit grid-center-vertically toolbar-label label">@Label.Text</span>

@code {
    [Parameter, EditorRequired]
    public ToolbarLabel Label { get; set; } = default!;
}
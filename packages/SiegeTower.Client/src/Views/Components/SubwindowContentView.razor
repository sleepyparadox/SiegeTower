@namespace SiegeTower.Client.Views.Components

<div class="grid-dock-content grid-overflow-x grid-overflow-y subwindow-content">
    @ChildContent
</div>

@code {
    [Parameter, EditorRequired]
    public SubwindowContent Content { get; set; } = default!;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
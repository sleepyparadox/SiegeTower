@namespace SiegeTower.Client.Views.Components

<button class="grid-line grid-center-vertically grid-width-fit grid-padded toolbar-button button" type="button">@Button.Text</button>

@code {
    [Parameter, EditorRequired]
    public ToolbarButton Button { get; set; } = default!;
}
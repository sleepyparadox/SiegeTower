@namespace SiegeTower.Client.Views.Components

@foreach (var node in Layout.Children.Values)
{
    <ControlLayoutNodeView Node="@node" />
}

@code {
    [Parameter, EditorRequired]
    public ControlLayout Layout { get; set; } = default!;
}